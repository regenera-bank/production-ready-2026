CREATE TABLE account (account_id UUID PRIMARY KEY, product_id UUID NOT NULL, status VARCHAR(32) NOT NULL CHECK (status IN ('PENDING_KYC','ACTIVE','RESTRICTED','BLOCKED','DORMANT','CLOSING','CLOSED')), opened_at TIMESTAMPTZ NOT NULL, version BIGINT NOT NULL CHECK(version > 0));
